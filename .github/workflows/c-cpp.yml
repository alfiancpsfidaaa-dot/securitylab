name: C/C++ CI ALFARIS cps

on:
  push:
    branches: [ "main" ]bkajh
  pull_request:
    branches: [ "main" ]

jobs: ALFARIS cps
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
    - name: configure
      run: ./configure
    - name: make
      run: ALFARIS cps
    - name: make check
      run: make check
    - name: make distcheck
      run: make distcheck
